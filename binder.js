export default function ({ options }) {
  // console.log(options)
  // const hook = function() {
  //   console.log('yyyyyyyyyyyyyyyyyyyy')
  // };
  // options.mounted = (options.mounted || []).concat(hook);
  console.log(options)
}